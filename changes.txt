diff --git a/functions.php b/functions.php
index f4556da..1530d44 100644
--- a/functions.php
+++ b/functions.php
@@ -185,10 +185,3 @@ function wps_get_fb_twitter_icons(){
 
     return $html;
 }
-
-
-///////////////////////////// Adding a new menu //////////////////////////
-register_nav_menus( array(
-    'primary' => esc_html__( 'Primary', 'wp-bootstrap-starter-child' ),
-    'drawer' => esc_html__( 'Drawer', 'wp-bootstrap-starter-child' ),
-));
\ No newline at end of file
diff --git a/header.php b/header.php
index e637907..04f8bae 100644
--- a/header.php
+++ b/header.php
@@ -46,19 +46,17 @@
                 </div>
 
                 <?php
-                    if( has_nav_menu( 'primary' ) ){
-                        wp_nav_menu(array(
-                            'theme_location'    => 'primary',
-                            'container'       => 'div',
-                            'container_id'    => 'main-nav',
-                            'container_class' => 'collapse navbar-collapse justify-content-start',
-                            'menu_id'         => false,
-                            'menu_class'      => 'navbar-nav wps-main-menu',
-                            'depth'           => 0,
-                            'fallback_cb'     => 'wp_bootstrap_navwalker::fallback',
-                            'walker'          => new wp_bootstrap_navwalker()
-                        ));
-                    }
+                    wp_nav_menu(array(
+                        'theme_location'    => 'primary',
+                        'container'       => 'div',
+                        'container_id'    => 'main-nav',
+                        'container_class' => 'collapse navbar-collapse justify-content-start',
+                        'menu_id'         => false,
+                        'menu_class'      => 'navbar-nav wps-main-menu',
+                        'depth'           => 0,
+                        'fallback_cb'     => 'wp_bootstrap_navwalker::fallback',
+                        'walker'          => new wp_bootstrap_navwalker()
+                    ));
                 ?>
                 
                 <ul class="navbar-nav mr-right wps-social-nav">
@@ -95,20 +93,10 @@
             </div>
             <div class="wps-drawer-scroller relative">
                 <div class="iscroll-wrapper">
-                    <?php
-                    if( has_nav_menu( 'drawer' ) ){
-                        wp_nav_menu(array(
-                            'theme_location'    => 'drawer',
-                            'container'       => 'div',
-                            'container_id'    => false,
-                            'container_class' => 'iscroll-scroller',
-                            'menu_id'         => false,
-                            'menu_class'      => 'drawer-menu',
-                            'depth'           => 0,
-                            'walker'          => new wp_bootstrap_navwalker()
-                        ));
-                    }
-                    ?>
+                    <div class="iscroll-scroller">
+                        <ul class="drawer-menu">
+                        </ul>
+                    </div>
                 </div>
             </div>
             <div class="wps-drawer-footer position-absolute" >
diff --git a/js/navigation.js b/js/navigation.js
index 9afdf06..f46e7c5 100644
--- a/js/navigation.js
+++ b/js/navigation.js
@@ -116,7 +116,7 @@ function setUpDrawerMenu( $ ){
             this.toggleDropdown = this.toggleDropdown.bind(this);
             this.setScrollerHeight = this.setScrollerHeight.bind(this);
 
-            if( this.menuElement.length<1 ){
+            if( this.originalMenuElement.length<1 || this.menuElement.length<1 ){
                 $('.wps-menu-toggle').hide();
                 return false;
             }
@@ -158,6 +158,7 @@ function setUpDrawerMenu( $ ){
 
 
         copyNav : function(){
+            var clonedMenu = this.originalMenuElement.clone();
             var editListClasses = function(){
                 $(this).removeClass('nav-item menu-item')
                 .find('a').removeClass('nav-link').removeAttr('data-toggle aria-haspopup').addClass('drawer-menu-item')
@@ -167,7 +168,9 @@ function setUpDrawerMenu( $ ){
                 .children('li').each( editListClasses );
             };
 
-            this.menuElement.children('li').each( editListClasses )
+            clonedMenu.children('li').each( editListClasses )
+
+            this.menuElement.html( clonedMenu.html() );
         },
 
 
diff --git a/screenshot.jpg b/screenshot.jpg
index 59e8fa1..6e9419e 100644
Binary files a/screenshot.jpg and b/screenshot.jpg differ
